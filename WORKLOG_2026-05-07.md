# BT-Player 工作记录（2026-05-07）

## 今日目标

- 排查并修复多磁链场景下“流地址与文件列表不一致”的问题。
- 将 Rust 依赖从“手改 cargo 缓存”升级为“本地可复现构建”方案。

## 今日完成

### 1) 多磁链串台问题修复（已验证）

- 问题现象：
  - `stream_url` 指向某个 torrent（如 `torrents/2`），但 `files` 列表来自另一个 torrent（如 Sintel）。
- 根因：
  - 业务层查询文件树时默认取 `torrents[0]`，未绑定本次解析得到的任务 ID。
- 修复内容：
  - 在 `src-tauri/src/lib.rs` 增加 `torrent_id_from_stream_url()`，从 `stream_url` 提取 `torrent_id`。
  - `handle_resolve` 在 magnet 分支改为按当前 `torrent_id` 查询文件树。
  - 在 `src-tauri/src/engine/mod.rs` 新增 `get_file_tree_by_id(torrent_id)`。
  - 保留 `get_file_tree()` 并优化默认行为为取最新任务 ID（max id）。
- 验证结果：
  - `Tears of Steel` 与 `Cosmos Laundromat` 在浏览器端均已恢复正常。
  - `stream_url` 与返回 `files` 列表对应同一 torrent，未再出现串台。

### 2) 本地可复现依赖方案落地（已生效）

- 新增 `src-tauri/vendor/`（`cargo vendor` 生成依赖快照）。
- 新增 `src-tauri/.cargo/config.toml`，强制优先使用本地 vendor。
- 新增 `src-tauri/local-patches/librqbit/`，保存本地补丁版 `librqbit`。
- 在 `src-tauri/Cargo.toml` 增加：
  - `[patch.crates-io]`
  - `librqbit = { path = "local-patches/librqbit" }`
- 更新 `src-tauri/DEPENDENCY_PATCH_PLAN.md` 为本地可复现流程文档。

## 编译与运行状态

- `cargo check --offline`：通过
- `cargo build --release --offline`：通过（存在非阻断 warnings）
- 线上服务重启后，API 与流服务可用，业务链路可正常播放测试磁链视频。

## 已知非阻断项（暂不处理）

- `local-patches/librqbit` 内部若干 warning（unused import/variable、lifetime 提示）。
- `src-tauri/src/ai_strategy.rs` 的 `FileAnalysis` / `analyze_files` 未使用 warning。

## 建议的后续动作（下次迭代）

1. 为 `/api/resolve` 增加结构化日志（包含 info_hash、torrent_id、耗时）。
2. 增加一条回归脚本：校验 `stream_url` 的 torrent_id 与 `files[*].stream_url` 一致。
3. 评估将 60s 磁链超时改为“前端可感知的阶段式提示”，降低误判为故障的概率。

