# 网络测试临时准备指南 (梅林 + MerlinClash 专项)

针对你在开发过程中需要同时保持 **AI 对话 (代理)** 和 **BT 下载 (直连)** 的需求，我们采取"端口级精细分流"方案。

## 1. 代码侧配置 (V1.2 已就绪)
`src-tauri/src/engine/mod.rs` 中的关键网络配置：
- **固定监听端口**: `50051` (TCP & UDP)
- **协议升级**: 启用了 `uTP` (UDP 协议)，在 NAT 穿透中比 TCP 性能更强。
- **UPnP 开启**: 应用自动向梅林路由器申请端口映射。
- **DHT 持久化**: 已启用，后续启动无需从零预热路由表。
- **Tracker 列表**: 已更新为活跃的公共 Tracker（共 7 个）。

`src-tauri/src/hls_engine.rs` (V1.2)：
- **浏览器 User-Agent**: 防止 CDN 拒绝请求。
- **重试机制**: 分片下载失败最多重试 3 次，间隔 1 秒。
- **并发控制**: 同时最多 5 个分片并行下载。

## 2. Clash 规则侧配置 (jms0224.yaml)

在 `# 1. 基础/本地直连` 区块中已添加：
```yaml
- DST-PORT,50051,DIRECT           # BT-Player P2P
- PROCESS-NAME,bt-player.exe,DIRECT  # BT-Player 进程兜底
```

**效果**：
- BT 引擎的 P2P 流量（端口 50051）→ DIRECT 直连公网
- AI 协作、浏览器等其他流量 → 继续走代理
- 本地 HTTP 流服务（8080）→ 已有 DIRECT 规则

## 3. 路由器侧操作
1. 将修改后的 `jms0224.yaml` 上传到 MerlinClash 面板。
2. 确认 UPnP 功能已开启。
3. 无需手动端口转发（UPnP 自动处理）。

## 4. 验证步骤
1. 启动应用：`npm run tauri dev`
2. 检查日志是否包含：`BT 引擎监听地址: 0.0.0.0:50051`
3. 解析一个热门磁链，观察是否有 `New peer connection` 日志。
4. **验证 AI 连通性**: 同时保持 AI 对话正常。
5. **验证 HLS 录制**: 输入 m3u8 地址，观察是否有重试日志（如有网络抖动）。

---
*这种方案实现了"上帝的归上帝，凯撒的归凯撒"——下载流量直连公网获取最大 Peer 数，协作流量继续走代理。*
*最后更新: 2026-02-25*
