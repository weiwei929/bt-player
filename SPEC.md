# BT-Player 技术规格说明书 (Technical Specification)

## 1. 项目愿景
打造一个**简约、极致性能、原生体验**的桌面全协议流媒体播放器。通过 Rust 语言的高并发处理能力和原生 GPU 硬解码技术，实现磁力链接 (Magnet) 和在线流媒体 (HLS/m3u8) 的“即入即播”，并支持“边看边存”的灵活资源管理。

---

## 2. 硬件基准与性能优化策略

本项目将针对以下硬件环境进行深度优化：

### 2.1 硬件配置 (当前基准)
*   **CPU**: AMD Ryzen 5 3600X (6C/12T) - 负责高并发 Peer 管理、DHT 路由及数据校验。
*   **GPU**: NVIDIA GeForce GTX 1660 - 核心任务：**NVDEC / NVENC 硬解码**，利用 GPU 驱动进行渲染，确保 4K HDR 播放。
*   **RAM**: 32GB - 用于大容量读写缓冲，减少磁盘 IO 压力。

### 2.2 性能优化点
*   **顺序下载 (Sequential Download)**: 强制优先下载视频索引（Header）和当前播放点后的数据块。
*   **并行校验**: 利用 Rust 的多线程能力，在后台并行进行数据块的 SHA-1 校验，不阻塞 UI 或播放。
*   **GPU 渲染路径**: 播放引擎强制使用 `vo=gpu` 或 `vo=gpu-next`，通过 Vulkan/D3D11 接口直接渲染。

---

## 3. 技术栈选型 (Technology Stack)

| 维度 | 技术选型 | 理由 |
| :--- | :--- | :--- |
| **桌面框架** | **Tauri 2.0 (Rust)** | 比 Electron 内存占用少 80%，对 Rust 库调用性能最佳。 |
| **磁链引擎** | **rqbit-core (Rust)** | Rust 编写的高性能 BT 引擎，原生支持流式播放调度。 |
| **播放核心** | **libmpv** | 业界公认最强的开源播放内核，支持几乎所有视频编码及硬件加速。 |
| **UI 界面** | **React + Tailwind CSS** | 响应式设计，易于实现美观的玻璃拟态 (Glassmorphism) 效果。 |
| **本地存储** | **SQLite (rusqlite)** | 存取播放列表、进度、配置信息。 |
| **AI 扩展接口** | **ONNX Runtime / JSON Hook** | **[预留]** 用于后续集成 AI 1, 2, 4 进阶模块。 |

---

## 4. 核心功能及实现逻辑

### 4.0 基础版 vs 进阶版 (Modular Architecture)
本项目采用“核心/插件”架构。基础版专注于稳定播放与流处理，进阶版通过扩展接口注入 AI 能力。

### 4.1 协议自动识别与解析 (Base)
*   **磁链协议**: 识别 `magnet:?xt=urn:btih:`，通过 `rqbit-core` 开启 P2P 调度。
*   **HLS 协议**: 识别 `.m3u8` 后端链路，直接通过播放内核加载。
*   **AI 预留接口**: 解析 Metadata 后触发 `onMediaAnalyzed` 钩子，由 AI 模块判断资源安全性。

### 4.2 磁链预览与点播 (Base)
*   **即时解析**: 用户输入磁链后，App 展示获取过程。
*   **资源过滤/选集**: 解析后展示资源明细，用户可勾选特定文件。

### 4.3 边看边存 (Base)
*   **磁链转储**: 播放时一键切换为全量下载。
*   **HLS 录制**: 支持对在线 m3u8 流进行录制保存。

### 4.4 播放控制与 AI 视频钩子 (Advanced Reserved)
*   **Frame Hook**: 在基础播放流中预留像素数据回调接口，用于进阶版 AI 实时识别并跳过广告。

### 4.5 个人媒体库与 AI 助理 (Advanced Reserved)
*   **基础库**: 记录历史与状态。
*   **AI 扩展**: 预留对话框 UI 接口，用于进阶版展示影片背景信息。

---

## 5. 阶段性路线图 (Roadmap)

### 第一阶段：核心引擎验证 (CLI PoC)
- [ ] 初始化 Rust 项目，集成 `rqbit-core`。
- [ ] 实现基础磁链解析，成功开启本地 HTTP Stream。
- [ ] 使用外部 VLC/mpv 验证流播放。

### 第二阶段：Tauri 框架与“边看边存”逻辑
- [ ] 配置 Tauri 2.0 环境，链接 Rust 后端与前端。
- [ ] 实现“资源预览选框”UI。
- [ ] 开发“流转储”逻辑控制台（控制下载优先级切换）。

### 第三阶段：mpv 原生渲染集成
- [ ] 在 Tauri 窗口中通过原生 FFI 嵌入 `libmpv` 渲染窗口。
- [ ] 实现自定义播放器控件（进度条、音量、全屏）。

### 第四阶段：媒体库与性能调优
- [ ] 实现 SQLite 本地库，记录下载历史与文件分类。
- [ ] 开启 GPU 硬解码功能。
- [ ] 界面美化与动效添加。

---

## 6. 开发环境要求
- **OS**: Windows (当前)
- **Rust**: 1.92+
- **Node.js**: v22+
- **C++ Build Tools**: 已安装
- **libmpv**: 需下载对应的 `.dll` 文件

---
*文档版本: 1.0.0*
*更新日期: 2026-02-14*
