# BT-Player 优化方向讨论记录

> **日期**：2026-05-07（背景与对外说明于 2026-05-08 增补；**第二节「提纯 → 解析」架构** 同日写入讨论稿。）  
> **性质**：方案探讨，非实施计划；可与 [UPGRADE_IMPLEMENTATION_GUIDE.md](./UPGRADE_IMPLEMENTATION_GUIDE.md) 对照阅读（后者为锁定原则后的实施指导）。  
> **读者**：熟悉 BT / 流媒体概念即可；**无需访问本仓库**亦可参与技术讨论。

---

## 一、面向外部讨论者的背景说明

### 1.1 项目是什么、解决什么问题

**BT-Player** 是一个以 **BitTorrent 磁力/种子** 为输入的 **边下边播** 类系统：用户给出资源标识后，系统在下载过程中即向播放端提供可读视频流，而不是等整文件落地后再播放。

项目的实现形态包含 **桌面客户端**（例如基于 Tauri / 本地播放内核）与 **服务端无头部署**（例如在 VPS 上只跑下载与 HTTP 输出、浏览器作为观看端）等；**本文讨论的优化以当前已跑通的「服务端 + 浏览器」链路为主要场景**，其中许多结论对桌面端同样适用（输出形态、缓存与体验策略）。

### 1.2 使用场景与目标（讨论时的约束口径）

| 维度 | 说明 |
|------|------|
| **使用者规模** | **个人使用**为主；优化目标**不**按多用户 SaaS、多租户隔离来设计。 |
| **产品取向** | **稳定**、**快速**、播放**流畅**、交互**友好**；愿为体验做工程投入。 |
| **探索性** | 项目承担 **BT / 视频领域新技术的实验沙盘**；**主线方案求稳**，前沿能力采用「评估后再试」的策略，避免未成熟技术动摇基础链路。 |

在阅读后文「混合网关 / WebRTC」等长线设想时，可将 **当前决策焦点** 理解为：**先把单用户、中继模式下的体验与资源治理做到极致**。

### 1.3 技术栈与链路（仅概念层，不依赖具体文件名）

为便于站外讨论对齐术语，下表描述 **逻辑组件** 与常见实现；实际仓库可能合并或拆分模块，但不影响方案讨论。

| 逻辑层 | 典型职责 | 备注 |
|--------|----------|------|
| **Ingress / 提纯** | 将原始粘贴内容规范为 magnet / 种子 URL 等 | 详见 **第二节** |
| **BT 引擎** | Magnet/种子解析、Peer 发现、按策略拉取分片 | 讨论稿中以 **librqbit** 为代表；等价能力也可由其他 BT 库承担。 |
| **顺序/流式读** | 保障播放点邻近数据优先、向 HTTP 侧暴露连续可读流 | 「边下边播」的关键调度所在。 |
| **媒体处理** | 容器级 **Remux**（不重编码）或直通 | 下文 **fMP4 + HLS** 方案主要落在此层。 |
| **HTTP 服务** | Range/分片 URL、播放列表（m3u8）等 | 浏览器通过 **hls.js** 或其它播放器消费。 |
| **本地存储** | 已下载分片与缓存目录 | 与 **缓存淘汰** 策略直接相关。 |
| **前端** | 粘贴链接、状态展示、播放器 | 与 **阶段式进度反馈** 强相关。 |

**端到端数据流（中继场景，概念图）**：

```
用户输入（Magnet / 种子 URL / 页面中的链接等）
    → 【可选显式层】链接提纯 / 规范化（见第二节）
    → 解析与任务创建（连接 DHT / Tracker，获取元数据）
    → BT 引擎从公网 Peers 拉取数据 ──→ 写入服务端本地缓存
    → 同一份数据经 Remux/封装后，以 HTTP（整流或分片）提供给浏览器
```

上图中「提纯」在架构上已与解析 **显式分拆**（第二节）；旧版讨论中曾将其隐含在「解析」一步内，易引起职责混淆。

### 1.4 当前已具备的能力与主要矛盾

**已跑通**：磁力（及种子）解析 → P2P 获取数据 → **通过 HTTP 面向播放端输出**，实现「边下边播」闭环（含在 VPS 上以无头服务运行的情况）。

**结构性矛盾**：在 **浏览器无法直接参与标准 BT swarm**（见 **第四节** 方案 B）的前提下，常见部署是 **服务端中继**：

```
Peers（公网 BT 客户端） ──数据──▶ 服务端（本项目） ──再转发──▶ 浏览器
```

由此带来 **同一条媒体流在公网路径上的双重经过**（简略称为 **双倍带宽压力**）、**多一跳延迟**，以及 **服务端磁盘随任务增长** 等问题。**第三节「方案 A」** 不试图消除「必须经过一台能讲 BT 的机器」这一事实，而是通过 **输出格式（fMP4 分片 + HLS）、缓存策略、可感知进度** 等，在既有中继架构下把 **可播性、稳定性与体验** 做到最好；**第四节「方案 B / 混合」** 则讨论能否在长期降低中继成本（对个人场景优先级可单独评估）。

### 1.5 输入侧的实际情况（与架构优化的关系）

站外讨论时容易默认「用户总是粘贴标准 `magnet:?`」，但实际使用中常见：

- **单文件种子**（一个主视频）；
- **多文件合集**（需在元数据层面选择播放目标）；
- **非直接磁力**：短链、跳转页、论坛/网盘说明页中嵌套的磁力或 `.torrent` 链接；
- **需从网页中提取** 的资源入口（维护成本与稳定性需在工程上单独分层）。

因此，在「极致中继」之外，还有一条与体验强相关的架构线：**入口层「链接提纯 / 规范化」后再进入 BT 解析**（**详细见第二节**），以降低无效重试、超时黑盒和日志噪声。落地顺序与原则另见 **UPGRADE_IMPLEMENTATION_GUIDE.md**；评审时可将 **「提纯模块」** 视为与 **fMP4+HLS、缓存淘汰** 并列的工程柱。

### 1.6 术语速查（避免歧义）

| 术语 | 含义 |
|------|------|
| **Remux** | 更换容器/封装，**不**重新编码视频轨（如 `-c copy`）。 |
| **Transcode** | 重新编码（耗时、吃 CPU/GPU，默认避免）。 |
| **fMP4** | Fragmented MP4，分片独立可解码，适合渐进播放。 |
| **HLS** | HTTP Live Streaming：`m3u8` 索引 + 多个媒体分片 URL。 |
| **Swarm** | 某一资源在 BT 网络中的全体 Peers。 |
| **中继** | 服务端从 BT 取数再推给仅支持 HTTP/Web 的播放端。 |
| **提纯（规范化）** | 将杂乱用户输入转换为 **解析核心** 可稳定消费的磁力/种子引用；见第二节。 |

---

## 二、入口架构更新：种子链接提纯 → 再进解析

> **共识**：将「用户提供的一串文本/URL」与「BT 元数据解析、会话创建」**拆成两阶段**：先经 **提纯（Ingress）** 模块输出规范输入，再由 **解析核心** 连 DHT/Tracker、拉起下载。已与 **方案 A（极致中继）** 并列列为升级主线的承重柱。

### 2.1 动机：为什么要从解析里拆开一层

在用户侧，资源入口往往不是「一串干净的 `magnet:?`」，而是：**短链、多层跳转页、合集种子、仅从网页正文/按钮里抠出来的磁力或 `.torrent` 链接**。若所有这些逻辑都和 **librqbit 解析、Peer 调度** 写在一起，会出现：

- **失败原因混杂**：说不清是「链接本身无效」「网络打不开页面」还是「没有 Peers」；
- **长尾超时**：入口处本应秒级给出的「无效」，被拖成长时间 **空白等待或笼统超时**（与 **第三节** 阶段式进度讨论中的痛点同源）；
- **演进成本**：换一个站点排版就要动 BT 核心，回归面过大。

**提纯模块**就是把「互联网上各种形态的入口」**收敛成少数几种机器可枚举类型**，再交给现有已跑通的解析链路。

### 2.2 架构位置与数据流

**调整前（概念上的一体化入口）**：

```
用户粘贴任意文本/URL ──► [解析 + BT 会话] ──► 后续下载与播放
```

**调整后（已定方向）**：

```
用户粘贴任意文本/URL
    ──► 【提纯模块】规范化 / 解压链 / 可选页面抽取
            ──► 规范中间表示（见 2.3）
    ──► 【解析核心】Metadata、选文件、创建任务（仅消费规范输入）
    ──► 下载、Remux、HLS、播放（与第三节方案 A 衔接）
```

提纯失败时 **应在本层快速返回**（带可分类错误，如「非 BT 链接」「页面无磁力」「短链解析失败」），**不占用** BT 会话与 DHT 重试预算；解析核心仅处理「已经是合法待解析目标」的情况。

### 2.3 提纯模块的职责边界（讨论稿级定义）

| 归属 | 做什么 | 典型不做 |
|------|--------|----------|
| **提纯** | 识别输入类型；展开短链；解析跳转得到最终 magnet / `.torrent` URL / 本地种子路径；**可选**：按可维护规则从 HTML 中提取链接 | 不替代 **DHT/Tracker** 元数据拉取；不负责 **Peer 连接** |
| **解析核心** | 对规范后的 magnet/torrent **做 BT 层解析**、文件列表、任务 ID、与现有 Stream 服务对接 | 不内置各论坛/网盘的页面抓取特例（由提纯适配器扩展） |

**与「多文件合集」的关系**：提纯可以只保证「指向正确的种子/magnet」；**默认播哪个文件**（最大视频、命名启发式、用户上次选择等）宜放在 **元数据解析之后的小模块**（「播放目标选择」），仍与 BT 核心 **解耦**，避免与 URL 层逻辑搅在一起。

### 2.4 规范中间表示（对外讨论可简称「契约」）

实施时需定稿具体字段；讨论层面可约定提纯输出至少区分：

- **Magnet URI**（含 `btih`，参数完整优先）；
- **HTTP(S) 指向的 `.torrent` 文件**（URL 已最终化，无短链）；
- **本地已存在的种子文件路径**（若部署形态支持上传文件）；
- **失败**：带 **原因类别**（链接格式、网络、页面无结果等），供 UI 与日志统一处理。

解析核心 **只接受** 上述成功分支之一，避免在核心层再写一套「猜用户到底贴了什么」。

### 2.5 网页与站点特例：工程现实

从网页提取链接 **无通用银弹**：站点改版会导致选择器失效。推荐形态：

- **通用层**：能处理的 **直接磁力、直接种子 URL、常见短链**；
- **插件/适配器层**：按 **域名或站点** 注册「提取规则或脚本」，独立版本化与测试；
- **失败体验**：明确提示「该页需手动复制磁力」优于长时间假死。

### 2.6 预期收益（评审时可对照）

- **更快**：无效输入在入口层结束，不进入解析与下载队列；  
- **更稳**：日志与监控可按 **ingress / parse / swarm / remux** 分层归因；  
- **更易迭代**：新站点、新短链服务只扩提纯侧，**不扩散** 对 BT 引擎的修改面；  
- **与主线一致**：和 **fMP4 + HLS、缓存淘汰、阶段式反馈** 同属「单用户中继场景下先把基础做到位」，不依赖 **第四节** 混合/WebRTC 路线。

---

## 三、方案 A：极致中继

本节对应 **§1.4** 中的中继矛盾：在保留 **Peers → 服务端 → 浏览器** 的前提下，通过 **输出格式、缓存与交互** 把边下边播做到极致；**入口侧** 与 **第二节** 提纯管线衔接。**不依赖** 读者了解仓库内模块划分。

### 1. 转码层 → 确定为 Remux Only

**决策：只做 Remux（容器转换），不做 Transcode（编码转换）。**

#### 核心问题：MP4 的 moov 陷阱

传统 MP4 的元数据索引（moov atom）在文件末尾，必须下载完才能播放，对长视频失去"边下边播"意义。

#### 解法：fMP4 分片流式 Remux

- 将 mkv 等容器**流式 Remux** 为 fragmented MP4（fMP4）分片
- fMP4 结构：`[ftyp][moov空壳][moof+mdat][moof+mdat]...`，每片段独立可播
- ffmpeg 流式读取 librqbit 的 HTTP 流，实时输出分片，无需完整缓存
- 浏览器端通过 hls.js 逐片加载播放

```
工作流：
librqbit 边下载 → ffmpeg 流式 remux (-c copy) → fMP4 分片(~4秒/片)
    → 轻量 HLS 服务 → 浏览器 hls.js 逐片播放
```

ffmpeg 参考：
```bash
ffmpeg -i http://127.0.0.1:9527/stream/torrents/0/stream/0 \
       -c copy \
       -f mp4 \
       -movflags frag_keyframe+empty_moov \
       -segment_time 4 \
       output.m3u8
```

#### 支持的格式边界

| 输入容器 | 视频编码 | 输出 | 行为 |
|---------|---------|------|------|
| mp4 | H.264 | 直接播 | 无需 remux |
| mkv/ts | H.264 | fMP4 | Remux |
| mkv/mp4 | HEVC | fMP4 | Remux，但提示桌面浏览器可能不支持 |
| 其他 | 其他 | — | **明确拒绝**，返回不支持的编码/容器信息 |

**原则：能播的稳定播，不能播的清楚告知原因。不转码，不浪费算力。**

---

### 2. 缓存淘汰

**决策：以新换旧，阈值控制。不做串流方案。**

- 缓存上限：5G（可配置）
- 检查时机：每次新种子添加后触发
- 淘汰规则：
  1. 扫描 bt_cache/ 所有子目录，取 mtime
  2. 当前有活跃 HTTP 连接的种子标记为"受保护"，不淘汰
  3. 超出阈值时，从最旧的未保护种子开始删除
  4. 删除到总大小 < 阈值 × 80% 时停止（留缓冲区，避免频繁触发）
- 实现：遍历 bt_cache/ 目录，stat 取 mtime 排序，无需额外数据库
- 不考虑串流/画面推送方案，不成熟，不尝试

---

### 3. 进度反馈

**决策：阶段式反馈是刚需，结合分片 remux 链路设计。**

当前问题：60秒超时内空白等待，用户无法判断是"慢"还是"挂了"。

#### 播放全链路阶段

```
① 解析磁链   → 获取种子元数据（文件列表、大小）
② 连接 Peers → 找到提供数据的节点
③ 缓冲分片   → 下载足够数据 + remux 出前 2-3 个 fMP4 片段（8-12秒视频）
④ 持续播放   → 流水线稳定运行
```

#### 状态与 UI 话术

| 阶段 | 状态标识 | UI 话术 | 补充信息 |
|------|---------|---------|---------|
| ① | `resolving` | 🔍 解析中… | 拿到元数据后立即显示种子名称和大小 |
| ② | `connecting` | 🔗 寻找资源… | 已连接 N 个 Peers / 下行速度 X KB/s |
| ③ | `buffering` | ⏳ 缓冲中… | 进度 ▓▓▓░░ 3/8 片段，预计 Xs |
| ④ | `playing` | ▶ 播放中 | ↓ X MB/s，缓冲余量 Ns，Peers: N |

#### 关键体验细节

**② 连接 Peers 阶段——资源可见性**
- 暴露 librqbit DHT 状态：已发现节点数、已连接 Peers 数、下行速度
- 10秒内 0 Peers 时主动提示："暂未找到资源，该种子可能较冷门，继续尝试中…（可随时取消）"
- 核心原则：焦虑来自未知，信息消除未知

**③ 缓冲阶段——与分片 remux 联动**
- fMP4 分片约 4 秒一片，缓冲够 2-3 片即可播放
- 进度展示：`缓冲中 ▓▓▓░░░░ 3/8 片段  预计 6s 后可播放`

**④ 播放中——持续状态可见**
- 底部状态栏：`▶ 播放中  ↓ 2.3 MB/s  缓冲 24s  Peers: 7`
- 缓冲余量低于 5 秒时自动提示：`⏸ 缓冲不足，暂停等待…  需要约 3s`

#### 完整体验流程

```
用户粘贴 magnet:?xt=urn:btih:abc123...
  → 🔍 解析中…
  → 📦 Tears of Steel (544.9 MB)
  → 🔗 寻找资源… 已连接 5 个 Peers
  → ⏳ 缓冲中 ▓▓▓░░░░ 3/8  预计 5s
  → ▶ 播放中  ↓ 1.8 MB/s  缓冲 32s  Peers: 9
```

---

## 四、方案 B：VPS 只做协调，P2P 下沉浏览器端

### 理想形态

VPS 只解析磁链、返回 metadata 和 peer 列表，真正 P2P 下载交给 WebTorrent。
VPS 零带宽、零磁盘，只做"介绍人"。

### 现实困境：协议不互通

```
BitTorrent 生态：TCP/UDP 协议，百万级 peers（qBittorrent, Transmission 等）
浏览器环境：只能用 WebRTC，且只能连 WebRTC 节点

这两个世界基本不互通。
```

WebTorrent 的尴尬——浏览器里跑 WebRTC 只能连其他 WebTorrent 浏览器节点，连不上真正的 BitTorrent swarm。
BT 资源的 peers 绝大多数是传统客户端，它们不说 WebRTC。
纯方案 B 在当前生态下几乎不可行——peer 池太小，冷门资源基本没源。

---

### 历史借鉴

#### 快播的机制——封闭生态 P2P 流媒体

```
发布者（媒体源）
    ↓ 上传种子/源
Tracker 服务器（中心调度）
    ↓ 分发 peer 列表
用户客户端 ←→ 用户客户端（P2P 传输）
```

关键设计：
- 自有协议 `qvod://`，不是标准 BitTorrent
- 客户端即节点，看的人越多速度越快
- 统一生态，所有用户用同一客户端，协议对等，无"异构"问题
- 边下边播 + 上传，下载到的数据块立即供他人获取

**快播能跑通的根本原因：封闭生态，所有节点说同一种语言。**

#### BT Sync（Resilio Sync）——纯 P2P 同步，去中心化

```
设备A ←→ 设备B ←→ 设备C
  ↑          ↑          ↑
  └──────────┴──────────┘
       DHT 发现彼此
```

关键设计：
- 基于 BitTorrent 协议但去掉 Tracker，纯 DHT 发现
- Secret 密钥控制访问——知道密钥才能加入同步
- NAT 穿透——STUN/TURN 直连优先，中继兜底
- 增量同步 + 端到端加密

**BT Sync 的启示：P2P 可以穿透 NAT，但需要中继作为兜底方案。**

---

### 可行方案：快播式混合网关

借鉴快播和 BT Sync，取两者之长：

```
BitTorrent Swarm (TCP/UDP)
  └─ peers: qBittorrent, Transmission, ...
       │
       ↓ librqbit（传统 BT 协议）
    VPS 混合网关
       │
       ↓ WebRTC
  ┌────┴────┐
浏览器A    浏览器B    浏览器C
（同时互连 P2P）
```

**VPS 角色转变——不再是"中继搬运工"，而是"协议翻译器 + 引导节点"：**

| 角色 | 做什么 | 带宽消耗 |
|------|--------|---------|
| 协议翻译 | 把 BT 数据转成 WebRTC 可用的格式推给浏览器 | 首次/冷门资源时较高 |
| 信令服务器 | 帮浏览器之间建立 WebRTC 直连 | 极低 |
| 兜底中继 | 浏览器之间无法直连时临时中继（类似 BT Sync 的 TURN） | 按需 |

**多人观看时的规模效应——快播优势再现：**

```
1人观看：VPS 从 BT 拿数据 → WebRTC 推给浏览器（= 方案A，无法避免）
3人观看同一资源：
  VPS 推流给浏览器A → A 同时通过 WebRTC 分享给 B 和 C
  VPS 带宽只承担 1/3，B/C 从 A 拿数据
100人观看：
  VPS 几乎不用推流，浏览器之间自组织 P2P 网络
  = 快播的"看的人越多越快"
```

#### 与快播/BT Sync 的对照

| 维度 | 快播 | BT Sync | 混合网关方案 |
|------|------|---------|------------|
| 协议统一性 | 自有封闭协议 | BitTorrent 协议 | BT + WebRTC 双协议 |
| Peer 来源 | 只有快播用户 | 只有密钥持有者 | BT 全网 peers + 浏览器 |
| 中心依赖 | Tracker 中心 | 几乎无 | 信令 + 兜底中继 |
| NAT 穿透 | 客户端处理 | STUN/TURN | WebRTC 内置 ICE |
| 规模效应 | 越多人越快 | 固定设备数 | **越多人越快**（快播优势） |

---

### 渐进式演进路径

**纯方案 B 不可行，但混合方案有前景，且可以渐进式实现：**

```
Phase 1: 方案 A（当前）          — VPS 中继，先能用
Phase 2: 加 WebRTC 信令          — VPS 做浏览器间介绍人
Phase 3: 浏览器间 P2P 传输       — 热门资源 VPS 减压
Phase 4: VPS 退化为纯信令 + 兜底  — 热门资源零带宽
```

每一步都是前一步的自然延伸，不需要推翻重来。
Phase 1 和 Phase 2 可以并行——方案 A 的 Remux + 缓存淘汰 + 进度反馈照做，同时架构上预留 WebRTC 信令接口。

---

## 五、总结

| 方案 | 可行性 | 适用场景 | 核心取舍 |
|------|--------|---------|---------|
| A：极致中继 | ✅ 立即可行 | 少人使用、冷门资源 | 简单可靠，但 VPS 带宽/磁盘是瓶颈 |
| B：纯 P2P | ❌ 当前不可行 | — | 协议不互通，peer 池太小 |
| 混合：A→B 渐进 | ✅ 最优长线 | 多人热门资源 | 复杂度高，但规模效应显著 |

**推荐策略：先做 A 做到极致，架构上预留 B 的接口，待时机成熟自然演进。**

**补充（个人使用场景下的评审口径）**：若为 **单人自用**，「混合方案」中 **多人观看带来的规模效应** 收益有限，讨论时可把 **方案 A + 入口提纯 + 缓存与阶段反馈** 视为 **近期主路径**；混合 / WebRTC 仍可作 **架构预留与长线选项**，而不必与当前里程碑强绑定。已定稿的实施原则见 [UPGRADE_IMPLEMENTATION_GUIDE.md](./UPGRADE_IMPLEMENTATION_GUIDE.md)。

---

*本文档仅为讨论记录，不作为实施指令。*
*日期：2026-05-07*

---

**实施指导（锁定原则、顺序与论证背景）**：见项目内 [UPGRADE_IMPLEMENTATION_GUIDE.md](./UPGRADE_IMPLEMENTATION_GUIDE.md)（2026-05-08 起维护）。
