# BT-Player 优化升级版：实施指导与论证说明

> **文档性质**：面向「下一步开发与落地」的指导文件；可作内部 checklist，亦可摘取「背景与结论」一节供外部评审。
>
> **成文日期**：2026-05-08
>
> **关联文档（事实源优先级）**：
> 1) 《BT-Player VPS 部署与评估报告》（当前部署事实源）  
> 2) [OPTIMIZATION_DISCUSSION.md](./OPTIMIZATION_DISCUSSION.md)（技术讨论与路线）  
> 3) 本文档后续维护的 VPS 基线附录（Caddyfile/systemd/防火墙/构建补丁/目录结构）
>
> **边界声明**：本地历史原型（如旧仓库中的 `ARCH.md`、`SPEC.md`、`src-tauri/*` 资料）仅作创意参考，**不作为当前 VPS 主线实施事实源**。
>
> **范围声明**：下文 **Phase 主线** 以「承接当前链路、加固基础」为优先；文中出现的「业界前沿」「混合网关」等仅作上下文，**不纳入本 Phase 实施范围**，采用「持续关注 → 书面评估 → 再决定是否试错」的流程。

---

## 1. 背景信息（便于外部论证）

### 1.1 项目定位（当前共识）

| 维度 | 说明 |
|------|------|
| **使用者** | 单人使用，不按多租户、多会话产品化设计 |
| **目标** | **稳定**、**快速**、播放**流畅**、交互**友好** |
| **角色** | 兼作 **BT / 视频相关技术的实验沙盘**：主线求稳；新技术先评估再决定是否集成 |

### 1.2 技术与运行现状（简述）

- 核心链路已通过验证：**磁力解析 → P2P 获取数据 → HTTP 面向播放端输出**（实际部署含无头 / VPS 等形态时，常见为 **Peers → 服务端 → 浏览器** 的中继结构）。
- 中继模式下的典型矛盾（讨论稿中已展开）：**带宽双份、延迟叠加、本地/远端磁盘持续增长**。本升级 **不试图用「未成熟串流方案」替代 BT 与本地缓存**，而是在 **输出形态、输入管线、缓存策略、可观测性** 上把基础打牢。

### 1.3 与讨论稿的关系

- [OPTIMIZATION_DISCUSSION.md](./OPTIMIZATION_DISCUSSION.md) 偏 **方案探讨**（含 fMP4、缓存、进度反馈、以及长线「混合网关」等）。
- **本文档**将其中 **已锁定** 的部分固化为 **实施原则 + 推荐顺序 + 验收关注点**，并补充 **错误分层、可观测、配置与回滚、安全底线**，避免实施阶段口径漂移。

### 1.4 技术选型成熟性（对外可引用的一句话）

- **fMP4 分片 + HLS（m3u8）** 属于当前点播与渐进式播放中 **工程上成熟、生态广泛** 的路径（与是否「营销意义上的最新」无关）；在 **仅 Remux（容器级重组）、不默认转码** 的前提下，利于 **稳定承接现有下载与 HTTP 源**，并与 `hls.js` 等播放器栈兼容。

---

## 0. 前置条件：VPS 阵地加固（必须先完成）

在开始做任何“体验层（例如 remux/HLS）”或“入口层优化”之前，先把 VPS 从“能跑通”升级为“可长期驻守”。这一阶段不改变主线技术路线，但会显著降低后续排障成本与偶发风险。

### 0.1 Caddy 入口面（安全 + 路由一致性）

- 后端服务继续 **仅监听 `127.0.0.1`**，公网只暴露 Caddy。
- 对 API 与流媒体路径加 **轻量鉴权**（例如 `basic_auth`），建议覆盖：
  - `/api/*`
  - `/stream/*`
- 确保路由转发不做会破坏后端路径的“剥前缀”行为；若使用 `handle_path` 等能力，必须以最终后端期望路径为准并在部署文档里写明。
- Caddyfile 以“可复现配置块”为单位存档：不要只保留片段或疑似损坏的块（否则迁移成本巨大）。
- 开启 access log 便于排障，但避免把鉴权 token 或敏感参数以明文形式完全写入日志（必要时做掩码）。

### 0.2 systemd 服务（重启策略 + 沙箱 + 权限收敛）

建议 systemd 单元满足至少这些约束（示例口径）：

- `User=btplayer`（低权限专用用户）
- `WorkingDirectory=/opt/bt-player`（或项目实际目录）
- `Restart=on-failure`、`RestartSec=3`
- `LimitNOFILE=65535`（按需要；BT/HTTP 并发可能受影响）
- 使用资源与权限收敛机制（视发行版与需求取舍），例如限制可写路径：
  - `ReadWritePaths=` 仅允许缓存目录与日志目录
  - 能用则配合 `ProtectSystem=` 等机制减少系统写入面

关键原则：**让“坏事”只发生在受控目录内**，避免错误配置导致系统盘被写爆/被污染。

### 0.3 防火墙与端口最小暴露（明确实际监听范围）

- 只开放必要的 UDP/TCP 端口，并在部署文档里写明：
  - 实际监听端口/范围（BT/DHT/Tracker/P2P 相关）
  - Caddy 公开端口（通常仅 80/443）
- 若使用 UFW/iptables：以“最小放行 + 可解释”作为验收标准。

### 0.4 可复现性（不依赖手动 cargo cache）

- 禁止“手改 cargo 缓存/临时下载后忘记固化”的不可控做法。
- 必须形成 **VPS 运行基线包**（可随版本归档），至少包含：
  - 完整 `Caddyfile`
  - 完整 `bt-player.service`（或等价 systemd unit）
  - 防火墙规则快照（如 `ufw status numbered` 输出）
  - 构建命令与构建环境说明（Rust/依赖版本）
  - `librqbit` 补丁来源与应用方式记录
  - VPS 目录结构与数据目录约定
  - 可回滚版本（tag/压缩包/二进制快照）
- 原则：任一新机器按“基线包”即可恢复同等部署，不依赖口头记忆。

### 0.5 缓存配额前置（磁盘是最主要的长期风险）

- 在 VPS 可用磁盘较小时（例如 45GiB 量级），缓存淘汰必须在早期就接入。
- **Phase 0 最低保护**：先落地“防写爆”措施（脚本/cron/systemd timer 任一种），保证超过阈值时可报警或自动清理。
- **Phase 1+ 精细治理**：再实现应用内缓存淘汰（活跃连接保护 + `mtime` + 回落到 80% 阈值）。
- 验收标准：连续使用多任务后磁盘不爆，且无需人工临时救火删目录。

### 0.6 观测面最小集（让故障可定位）

- 至少保留以下可定位信息（可来自 systemd/journalctl 与 Caddy）：
  - 后端日志（含 request id / torrent id / 阶段迁移）
  - Caddy access/error log
  - `journalctl -u bt-player` 可直接用于排障
- 建议提供一个简易健康检查接口（例如 `/health`），用于确认后端存活与主要依赖状态。

## 2. 升级版整体架构（原则层）

以下 **四条** 共同构成「优化升级版」的闭合规划：前三条为 **技术承重**，第四条为 **体验与可维护性承重**。

### 2.1 柱 A：输出形态 — fMP4 分片 + HLS

**锁定结论**

- **承载格式**：**Fragmented MP4（fMP4）** 作为分片，避免传统 MP4 **moov 居后** 导致长视频无法真正「边下边播」的问题。
- **分发形态**：**HLS**（`m3u8` + 分片 URL）；播放侧以 **hls.js**（或等价能力）为主。
- **处理策略**：在兼容前提下 **Remux（如 `ffmpeg -c copy`）**；**不**为追新而默认 **Transcode**。
- **格式边界**（与讨论稿一致）：能稳定播的明确支持；**HEVC 等浏览器敏感组合**须 **显式提示**；无法支持的编码/容器须 **拒绝并说明原因**（见第 4 节错误分层）。
- **上线策略**：HLS/fMP4 作为新增输出路径，必须通过 **feature flag** 启用；旧 HTTP stream 输出保留为回退路径，直到典型资源验证稳定。

**承接当前成果**

- 优先 **增量演进**：在现有「BT → HTTP 可读流」之上，增加 **分片生成与 Playlist 服务**；避免一次性推翻下载与 session 管理。

### 2.2 柱 B：输入管线 — 种子链接「提纯」→ 再进入解析

**锁定结论**

- 单独 **Ingress / 提纯模块**：将 **多样来源**（纯 magnet、含跳转的 URL、页面内嵌磁力/种子链接、包内单文件场景等）规范为 **解析层可稳定消费的输入**（类型 + 规范化 URL / infohash / 本地路径等）。
- **解析与任务核心** 只消费提纯结果；**站点特例、页面抽取规则** 放在适配层，**避免污染** BT 核心逻辑。

**预期收益**

- **更快失败、更少无效重试**：在入口层即可判定「不可解析链接」而非长超时悬挂。
- **更稳的分类与日志**：网络问题、冷门无 peer、格式不支持可分层归因（见第 4 节）。

**可选细分（实施时按需打开）**

- **URL 层提纯**：短链解开、磁力/种子直链规范化。
- **页面层提纯**：按域名/页面的 **可维护适配器**（接受改版维护成本）。
- **元数据后的播放目标选择**（包种子）：启发式默认选最大视频文件等 —— 仍可置于 **独立于 BT 会话** 的小模块中，以保持核心简洁。

### 2.3 柱 C：本地缓存策略（沿用讨论稿，强制执行）

以下为 **不可分割** 的整体策略（数值可配置，**语义不变**）。

| 项 | 规则 |
|----|------|
| **上限** | 总占用上限（示例 **5 GiB**，必须 **可配置**） |
| **触发时机** | **每次新增种子** 后加入检查逻辑 |
| **统计方式** | 扫描 **`bt_cache/`**（或等价根目录）下各子目录，用 **`mtime`（及 stat 结果）排序**；**不依赖额外数据库** 仅存淘汰元数据 |
| **保护规则** | 存在 **活跃 HTTP 连接**（为该种子/任务服务）的目录 **不参与淘汰** |
| **淘汰顺序** | 超限时，从 **最旧且未受保护** 的条目开始删除 |
| **停手阈值** | 删至总占用 **&lt; 上限 × 80%** 即停止（缓冲带，防止抖动） |
| **排除范围** | **不采用** 「串流/画面推送代替本地缓存」等未列入本项目的方案 |

### 2.4 柱 D：体验 — 阶段式进度与状态可见（强烈推荐与柱 A/B 同步交付）

讨论稿中的阶段划分在实施上视为 **硬性 UX 契约**，与「流畅感、可解释性」目标一致：

1. **`resolving`** — 解析磁链 / 元数据  
2. **`connecting`** — 连接 peers（可暴露 DHT/peer 数、下行速度等）  
3. **`buffering`** — 凑够首播所需 **fMP4 分片**（如首 **2～3** 片）  
4. **`playing`** — 持续流水线；低缓冲时明确提示等待

**文案与阈值示例**（可微调，但必须保留「阶段 + 可操作信息」）：

- **10 s 内 0 peers** 等：提示「可能冷门、仍在重试、可取消」类信息，减少「未知焦虑」。

---

## 3. 推荐实施顺序（降低返工）

建议 **默认按序** 推进；若资源并行，**柱 B 契约** 与 **柱 A 播放 URL 形态** 需先对齐，避免联调断裂。

0. **事实层**：以《BT-Player VPS 部署与评估报告》确认当前状态，补齐 VPS 基线附录（Caddy/systemd/防火墙/目录/构建/补丁/回滚）  
1. **加固层（Phase 0）**：先做安全、可复现、观测面、最低磁盘保护  
2. **优化层 A**：提纯模块输入/输出契约（类型枚举、错误码、单测）  
3. **优化层 B**：解析层仅依赖契约（现有 rqbit / 会话逻辑尽量少动）  
4. **优化层 C**：HLS 端点（feature flag 启用），并保留旧输出回退路径  
5. **优化层 D**：应用内精细缓存淘汰（活跃连接保护 + `mtime` + 80% 停手阈值）  
6. **优化层 E**：阶段式状态机 + UI 文案与 A/B/C 联调  
7. 收尾：可观测性细化、配置治理与回滚演练

---

## 4. 错误与失败：分层约定（建议写进 API / 日志规范）

实施阶段建议固定 **类别**，便于 UI 一句人话 + 日志可检索：

| 类别 | 典型原因 | 对用户呈现原则 |
|------|----------|----------------|
| `ingress_invalid` | 提纯后仍非合法 magnet/torrent | 明确「链接无效 / 请换源」 |
| `fetch_failed` | 短链、种子 URL、页面拉取失败 | 「网络或源站不可用」 |
| `resolve_failed` | 元数据/解析失败 | 与上区分，避免统称「坏了」 |
| `swarm_cold` | 长时间 0 peer 或极低速 | 与讨论稿「冷门提示」一致 |
| `remux_unsupported` | 容器/编码组合不支持且不做转码 | **说明原因**，不静默失败 |
| `storage_exhausted` | 磁盘或缓存策略触顶 | 提示清理或调大上限 |
| `internal` | 断言外未预期错误 | 日志带 request id / torrent id，UI 简短道歉 + 重试 |

HTTP 状态码与错误体字段可在实现时定稿，但 **类别枚举应稳定**，避免前端不断适配。

---

## 5. 可观测性（最小集）

个人项目仍建议 **结构化日志**（或等价 tracing 事件），否则「偶发卡顿」难复盘：

- **阶段迁移** 时间戳：`resolving` → … → `playing`  
- **关键指标**：**首个分片就绪时间**（TTFB 到可播）、当前 peer 数、下行速率、缓存目录占用  
- **关联 id**：torrent / 任务 id、提纯来源类型（不记录敏感完整 URL 若担心泄露，可记 hash 或截断）

无需完整监控大盘，但要能 **对照第 2.4 节阶段** 定位瓶颈。

---

## 6. 配置与回滚

- **可配置项**（至少）：缓存上限、分片时长、HLS 相关路径前缀、超时与重试上限。  
- **特性开关**（推荐）：例如 **「旧：单一流式输出」** 与 **「新：fMP4 + HLS」** 可切换，便于回归与排障。  
- **变更记录**：重大行为变更在 [ARCH.md](./ARCH.md) 或本文件追加 **修订小节**（日期 + 要点）。

---

## 7. 安全与滥用（底线）

即便 **单人使用**，若服务端对公网暴露，仍建议规划内包含：

- **鉴权**（令牌、IP 限制、或反向代理层限制）  
- **基础限流**（防误扫、防滥用）  
- **禁止** 将本服务 **误用为开放 HTTP 代理**（配置与文档中写明边界）

细节随部署方式调整，但 **意识与默认配置** 应纳入本升级规划。

**最小验收条款（建议强制化）**：

- 未登录访问 `/api/*` 返回 401（或等价拒绝）。
- 未登录访问 `/stream/*` 返回 401 或不可获取媒体内容。
- 后端服务端口不可被公网直接访问（仅本机回环可达）。
- Caddy access log 可追溯来源 IP、路径、状态码。
- 流媒体任务设置并发/数量上限，避免单次误操作拖垮主机。

---

## 8. 明确不在本 Phase 的范围（减少论证歧义）

以下 **不列为当前实施承诺**，仅在持续跟踪后单独开题评估：

- **纯浏览器侧 BT swarm**（WebTorrent 与传统 BT 异构互联互通问题，讨论稿 **方案 B 纯路线**）  
- **混合网关 / 浏览器间 WebRTC 大规模分流**（讨论稿长线 **Phase 2+**）  
- **为多用户做多租户隔离与计费级 SLA**

---

## 9. 验收关注点（自检清单）

- [ ] 典型 **单文件种子**、**多文件包种子** 均能完成：**提纯 → 解析 → HLS 可播**  
- [ ] **首播** 在可接受的 peer 条件下，缓冲阶段有可理解的 **分片进度**  
- [ ] **缓存**：新增任务触发淘汰逻辑；**播放中目录不被删**；降至 **80%** 阈值停手  
- [ ] **不支持格式** 有明确错误或提示，**不静默黑屏**  
- [ ] 日志可回答：卡在哪一 **阶段**、**首个分片** 耗时、是否 **swarm_cold**

---

## 10. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-08 | 初稿：沉淀讨论结论与实施顺序，补充论证用背景与安全、可观测等底线 |
| 2026-05-08 | 按 VPS 主线边界修订：新增“事实源优先级”、Phase 0 基线包、缓存分层、HLS feature flag 回退、安全验收条款与三层实施顺序 |

---

*本文档为指导与论证材料；具体接口字段与代码路径以评审后实现为准。*
