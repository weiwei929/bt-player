# BT-Player 后续路线论证：rqbit 主线、qBittorrent 备线与云缓存层

> 日期：2026-05-08  
> 文档性质：Phase 1 前置路线论证，暂不作为实施指令  
> 当前状态：仅用于评估、定调与后续决策；正式推进时间另行决定

---

## 1. 当前边界与事实源

BT-Player 当前已完成 VPS Phase 0 基线加固：服务可运行，API 与 Stream 公网入口已受控，Caddy、systemd、cache_guard 与验收记录已沉淀。

本阶段讨论不再以本地旧 Tauri 项目作为当前事实源。旧项目只保留为创意缘起和历史原型参考；当前事实源优先级为：

1. VPS 实际运行状态；
2. `BT-Player VPS Phase 0 验收记录.md`；
3. 本文档及后续路线评估补充。

本文档只讨论后续路线，不进入 HLS/fMP4、PikPak 实时播放、WebRTC、Jellyfin/Plex、Debrid 等实现。

---

## 2. 总体定位

BT-Player VPS 不应被设计成新的大一统媒体平台，而应先成为一个稳定的 **BT/magnet 即时播放前哨**。

整体媒体体系可按角色划分：

| 模块 | 定位 | 当前策略 |
|------|------|----------|
| `webproxy-ai` | 网页 / 站点 / RSS / 页面资源发现前哨 | 纳入入口层，但不并入 BT-Player |
| `BT-Player VPS` | magnet / torrent 即时探测与边下边播前哨 | 当前主线 |
| `rqbit / librqbit` | BT 流式播放核心能力 | 主线技术基座 |
| `qBittorrent-nox + Web API` | 成熟 seedbox / 稳定下载备线 | 备线与对照组 |
| `PikPak` | 云端离线、温层资产池、冷门资源兜底 | 人工或半自动旁路优先 |
| `Dropbox` | 精选完成品归档 | 不进入 BT 热链路 |

核心链路目标：

```text
发现入口 -> 快速判断 -> 即时播放 / 转存 -> 资产入库 -> 稳定播放 / 长期归档
```

---

## 3. rqbit / librqbit 主线评估

### 3.1 定位

rqbit / librqbit 最贴近当前 BT-Player VPS 的核心目标：对 magnet 或 torrent 进行解析、连接 peers，并通过 HTTP stream 向浏览器提供可播放数据。

它适合承担：

- 即时 magnet 解析；
- peer / DHT / tracker 探测；
- 边下边播；
- 临时缓存；
- 状态反馈；
- 后续 fMP4/HLS 输出层的上游数据源。

### 3.2 优势

1. **目标贴合**  
   rqbit 本身就接近“torrent -> HTTP stream”的模型，比通用下载器更适合即时播放。

2. **体验路径短**  
   用户输入 magnet 后，可以直接进入解析、选文件、播放，不必先完整下载。

3. **便于做阶段状态**  
   可围绕 `resolving / connecting / buffering / playing` 建立状态机和 UI 提示。

4. **适合作为实验沙盘**  
   后续 Remux、缓存策略、冷门资源判断、转存 PikPak 等逻辑都可围绕它扩展。

### 3.3 风险

1. **上游活跃变动带来的可维护性风险**  
   当前已出现过 librqbit 编译兼容问题，说明深度嵌入库会承担上游 API 变化成本。

2. **依赖补丁治理需要持续维护**  
   Phase 0 已通过 vendor 与 local patch 固化依赖，但这只是当前可复现快照，不是长期治理终局。

3. **任务管理能力不如成熟下载器**  
   若未来需要复杂队列、做种、限速、分类、长期下载，rqbit 可能不是最省心方案。

### 3.4 当前结论

rqbit / librqbit 继续作为近期主线，但应保持克制：

- 先巩固“即时探测 + 临时播放 + 状态可见”；
- 不急于把它扩展成完整下载管理器；
- 保留 qBittorrent-nox 作为备线；
- 继续维护依赖补丁快照，避免回到手改 cargo cache。

---

## 4. qBittorrent-nox + Web API 备线评估

### 4.1 定位

qBittorrent-nox 更适合作为“稳定下载与长期任务管理”能力的备线，不直接取代 BT-Player 的即时播放前哨角色。

### 4.2 优势

1. **成熟稳定**：seedbox 场景广泛验证，资料丰富。  
2. **任务治理能力强**：队列、分类、限速、做种、规则化更完善。  
3. **故障隔离价值高**：可作为 rqbit 主线异常时的降级与对照组。

### 4.3 代价

1. **即时播放路径较长**：通常先下载再播放，体验链路不如 rqbit 直接。  
2. **双栈维护成本**：主线 + 备线并存会增加运维复杂度。  
3. **产品抽象要求更高**：需要上层统一任务模型，避免逻辑分叉。

### 4.4 当前结论

qBittorrent-nox 不作为主线替代，而作为“稳定下载备线 + 对照组”保留：

- 主线故障时可快速切换保障可用性；
- 在复杂长期下载任务上承接压力；
- 为后续架构决策提供客观对照数据。

---

## 5. 云缓存层（PikPak）角色定位

### 5.1 定位

PikPak 在本路线中定位为：**云端缓存/归档/冷门资源兜底层**，不是当前实时播放路径的一部分。

### 5.2 当前策略

- 以人工或半自动流程优先；
- 仅预留任务状态、资源 ID、导出接口；
- 不在当前阶段接入实时播放依赖。

### 5.3 风险提示

- 第三方依赖、付费与可用性不可控；
- 若过早深绑定会削弱自托管主线确定性；
- 需先明确资产分层策略，再做自动化闭环。

---

## 5.5 Dropbox 归档层（补充）

Dropbox 仅承担“**精选完成品归档层**”角色，不参与 BT 热链路与即时播放路径。

当前约束：

- 不接入 magnet 解析、peer 调度、边下边播；
- 不替代 VPS 侧缓存治理；
- 仅用于人工筛选后的长期留存与跨设备取用。

---

## 6. 建议的阶段化推进（建议，不是指令）

### Phase 1A：主线巩固（rqbit 前哨）

- 完成应用级缓存治理（替代单纯依赖 cache_guard）；
- 巩固可观测性与阶段状态（阶段、耗时、失败类型）；
- 完成入口提纯（链接规范化）与错误分层。

### Phase 1B：备线建档（qBittorrent）

- 建立最小备线 PoC（不接入主入口）；
- 打通 Web API 基础操作（创建任务、查询状态）；
- 与主线在同样样本上做稳定性对照。

### Phase 1C：云层接口收敛（PikPak 占位）

- 保持仅任务接口与状态流；
- 明确导出成功/失败分类；
- 不进入实时播放链路。

---

## 7. 决策门槛（建议纳入评审）

当满足以下门槛再进入下一阶段：

1. **可用性门槛**：主线 7 天连续运行无严重中断。  
2. **可观测门槛**：任一失败可在日志中 5 分钟内定位阶段与类型。  
3. **资源门槛**：缓存策略稳定，不出现磁盘突发写满。  
4. **回滚门槛**：每次变更具备可验证的回退路径。  
5. **安全门槛**：公网入口保持鉴权与最小暴露原则。

---

## 8. 我的建议（基于现状）

1. **继续坚持“rqbit 主线 + qBittorrent 备线”双轨，但分时推进**  
   先把 rqbit 主线做到可解释、可恢复，再做备线常态化；不要同步扩张两条线。

2. **尽快把“缓存治理”从脚本兜底升级到应用策略**  
   cache_guard 是 Phase 0 的必要保险，但不是最终方案；优先级应高于任何新播放特性。

3. **把“路线争议”转化为可量化对照**  
   用统一样本比较 rqbit 与 qBittorrent 的首播耗时、失败率、资源占用，减少主观争论。

4. **云缓存层维持“接口先行、业务后置”**  
   先规范任务状态与可追踪性，再决定是否扩大自动化，不要提前把播放链路绑到第三方。

5. **保持边界纪律**  
   在 Phase 1 决策冻结前，不进入 HLS/fMP4 实施，不进入 PikPak 实时播放，不进入 WebRTC 混合网关。

---

## 9. 边界重申

- 当前文档仅为路线论证，不是实施指令。  
- 当前事实源以 VPS 现网与验收记录为准。  
- 本地旧项目代码仅作历史参考，不用于覆盖现网事实。  
- 后续若进入实施，需另行形成可执行任务单与验收标准。

